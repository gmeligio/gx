[package]
name = "gv"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
glob = "0.3"
anyhow = "1"
regex = "1"

[dev-dependencies]
tempfile = "3"
