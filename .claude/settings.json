{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "hook=\"$(git rev-parse --git-path hooks)/pre-commit\"; if [ ! -f \"$hook\" ]; then mise trust >/dev/null 2>&1; mise run setup >&2 && echo 'prek git hooks installed.'; else echo 'prek git hooks already installed.'; fi"
          }
        ]
      }
    ]
  }
}
